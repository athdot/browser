function search(){
var input = document.getElementById('search').value;
var length = input.length
var run = 0
var woobsat = 0
while(run<length){
if(input.substring(run,run+1) === "."){
if(input.substring(run,run+4) === ".com"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".org"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".net"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".edu"){
var woobsat = 1
}
if(input.substring(run,run+5) === ".html"){
var woobsat = 1
}
if(input.substring(run,run+3) === ".js"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".bat"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".txt"){
var woobsat = 1
}
if(input.substring(run,run+3) === ".io"){
var woobsat = 1
}
if(input.substring(run,run+3) === ".de"){
var woobsat = 1
}
if(input.substring(run,run+3) === ".py"){
var woobsat = 1
}
if(input.substring(run,run+5) === ".json"){
var woobsat = 1
}
if(input.substring(run,run+4) === ".css"){
var woobsat = 1
}
 if(input.substring(run,run+3) === ".ae"){
var woobsat = 1
}
 if(input.substring(run,run+3) === ".ar"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".at"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".bo"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cm"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cn"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cg"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".hr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ec"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".eu"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".hk"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".co"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".id"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".jp"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".lv"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".mx"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".nl"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ps"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ph"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ru"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".sk"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".es"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".tw"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ua"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".uy"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".uz"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".al"){
var woobsat = 1
}
  if(input.substring(run,run+5) === ".aisa"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".bb"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".br"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ca"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cz"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".eg"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".fi"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".gr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".is"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ie"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".jo"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".my"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".mx"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".no"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pa"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pl"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".qa"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".sl"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".si"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".se"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".tr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".uk"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".uy"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ve"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ag"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".au"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".be"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".bg"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cl"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".cd"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".hr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".dk"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".eg"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".fr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ht"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".in"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".it"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".la"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".mr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".me"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pk"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pe"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".pt"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ro"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".sg"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".kr"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".ch"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".tv"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".us"){
var woobsat = 1
}
  if(input.substring(run,run+3) === ".vn"){
var woobsat = 1
}
  if(input.substring(run,run+4) === ".biz"){
var woobsat = 1
}
   if(input.substring(run,run+5) === ".mobi"){
var woobsat = 1
}
   if(input.substring(run,run+7) === ".travel"){
var woobsat = 1
}
   if(input.substring(run,run+5) === ".info"){
var woobsat = 1
}
   if(input.substring(run,run+3) === ".pw"){
var woobsat = 1
}
   if(input.substring(run,run+4) === ".int"){
var woobsat = 1
}
   if(input.substring(run,run+4) === ".tel"){
var woobsat = 1
}
   if(input.substring(run,run+4) === ".xxx"){
var woobsat = 1
}
}
var run = run+1
}
if(woobsat === 1){
var location = input
}
if(woobsat === 0){
var location = ("https://www.google.com/?safe=active&ssui=on#safe=active&q=" + input + "&*")
}
document.getElementById('content').setAttribute('src',location);
document.getElementById('search').setAttribute('value',location);
}
